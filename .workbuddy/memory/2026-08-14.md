# 2026-08-14 工作日志

## 双电机 PID 闭环（阶段1）落地
- AI_deploy 工程位于 `C:\Users\123\Desktop\AI_deploy`。
- 新建 `Doc/Pinout.md`：引脚/定时器单一配置源（电机 PWM/方向/STBY、编码器 TIM3/4、TIM7 1ms、KEY、RGB）。
- 新建 `Components/Motor/motor.h` + `motor.c`：TB6612 驱动（TIM1_CH1 PA8 / CH2 PE11 + AIN1/AIN2/BIN1/BIN2/PB12 STBY）、TIM3(B 电机 PB4/5)/TIM4(A 电机 PB6/7) ×4 编码器、增量式 PI 速度环 + 位置环级联、TIM7 1ms 控制环 `Motor_1ms_Handler()`。
- 接线点：`stm32h7xx_it.c` TIM7_IRQHandler 调 `Motor_1ms_Handler`；`main.c` USER CODE 2 调 `Motor_App_Init`；`freertos.c` StartMotorTask 读 PC13 KEY 切换运行/刹车 + 每秒打印速度/位置/RPM。
- Keil `MDK-ARM/STM32H743VIT6.uvprojx`：新增 Motor 组 + IncludePath `../Components/Motor`。
- 验证：用 `/d/ST/STM32CubeCLT_1.18.0` 的 arm-none-eabi-gcc 对 motor.c 与 3 个 Core 文件做了 `-fsyntax-only`，全部 EXIT=0（项目真实 include 集）。
- 注意：H7 TIMxCLK=240MHz（APB 120MHz ×2，因 APB 预分频≠1）；SYS 已 Serial Wire，PB3/PB4 已释放；TIM1 为高级定时器，HAL_TIM_PWM_Start 已自动置 MOE，无需手动使能。
- 待上电整定：VELOCITY_KP/KI、POSITION_KP、`ENCODER_PPR`（TT 电机按 11 估，需按实物校准）。

## 下一步（阶段2）
- 加 MPU6050（GY-521，I2C2）做角度环，级联到速度环形成平衡小车直立控制。

## Components 目录风格统一
- 统一约定：每个组件下分 `Inc/`（头）+ `Src/`（源）；板级驱动 `BSP_` 前缀，纯软件模块小写。
- 重排：`BSP/{Inc,Src}`（BSP_GPIO/USART/W25Q64）、`Motor/{Inc,Src}`（motor）；新建 `Components/AI/Inc` 收纳 `model_weights.h`/`test_dataset_processed.h`/`test_dataset_raw.h`（原误放在 BSP）。
- Keil `uvprojx`：IncludePath `BSP`→`BSP/Inc`、`Motor`→`Motor/Inc`，新增 `AI/Inc`；BSP/Motor 组 FilePath 改到 Src/Inc，BSP 组补入原缺失的 `BSP_GPIO.h`/`BSP_USART.h`，数据头移至新 AI 组。
- 验证：arm-none-eabi-gcc `-fsyntax-only`（更新后 include 集）对 motor.c + 3 个 Core 文件全部 EXIT=0。
